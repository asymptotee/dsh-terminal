/**
 * Team coordination types. With the shared task board removed (message-only
 * coordination), this module no longer declares a task payload type or any
 * upstream event/projection merges — teammates coordinate purely through
 * `team_send` messages.
 * @module dsh-terminal/src/team/types
 */

export {}
